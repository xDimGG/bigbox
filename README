# BigBox File Storage

This is a file storage app written in Go for a YouTube series.

## Requirements

PostgreSQL Server. Can be installed locally or run Docker using this command

```
docker run -e POSTGRES_PASSWORD=password -p 5432:5432 postgres
```